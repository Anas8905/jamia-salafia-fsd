<template>
	<div class="relative w-full h-screen overflow-hidden">
		<NavBar></NavBar>
		<div class="duration-1000 ease-in-out">
			<img
			:src="images[currentIndex]"
			:key="currentIndex"
			class="w-full h-full object-cover absolute top-0 left-0"
			alt="..."
			/>
		</div>
		<div class="absolute left-1/2 bottom-10 transform -translate-x-1/2 hidden sm:block">
			<div class="flex justify-between">
				<div id="card" class="text-white border border-gray-500 bg-gradient-to-b from-black to-transparent hover:bg-black hover:bg-opacity-50 rounded-lg">
					<a href="#" class="block p-6 shadow">
					<h5 @click="languagesStore.changeLanguage()" class="mb-2 text-2xl font-bold tracking-tight text-white">{{ $t("fatwaheading") }}</h5>
					<p class="font-normal text-white">Here are the biggest enterprise technology acquisitions of 2021 so far.</p>
					</a>
				</div>
				<div id="card" class="text-white border border-gray-500 bg-gradient-to-b from-black to-transparent hover:bg-black hover:bg-opacity-50 rounded-lg mx-4">
					<a href="#" class="block p-6 shadow">
					<h5 class="mb-2 text-2xl font-bold tracking-tight text-white">Noteworthy technology</h5>
					<p class="font-normal text-white">Here are the biggest enterprise technology acquisitions of 2021 so far.</p>
					</a>
				</div>
				<div id="card" class="text-white border border-gray-500 bg-gradient-to-b from-black to-transparent hover:bg-black hover:bg-opacity-50 rounded-lg">
					<a href="#" class="block p-6 shadow">
					<h5 class="mb-2 text-2xl font-bold tracking-tight text-white">Noteworthy technology</h5>
					<p class="font-normal text-white">Here are the biggest enterprise technology acquisitions of 2021 so far.</p>
					</a>
				</div>
			</div>
		</div>
	</div>
  </template>
  
  <script>
  import HelloWorld from "@/components/HelloWorld.vue";
  import NavBar from "@/components/NavBar.vue";
  import { changeLanguages } from '../stores/changeLanguage'

  export default {
	name: "HomeView",
	components: {
		HelloWorld,
		NavBar
	},
  
	data() {
	  return {
		languagesStore: changeLanguages(),
		currentIndex: 0,
		images: [
			require("../../public/img/jamia/pic3.jpeg"),
		],
	  };
	},
  
	mounted() {
	  // Set interval to change image every 5 seconds
	  setInterval(this.changeImage, 8000);
	},
  
	methods: {
	  changeImage() {
		// Increment currentIndex, reset to 0 if reached the end
		this.currentIndex = (this.currentIndex + 1) % this.images.length;
	  },
	},
  };
</script>
  